# Rust build output
/target

# Packaging output (the .app / .dmg are built by scripts/package.sh)
/dist

# Rust backup files
**/*.rs.bk

# Profiling output (cargo flamegraph / perf / sample)
/flamegraph.svg
/perf.data
/perf.data.old

# Demo video (upload material rendered from assets/demo.gif, not shipped in the repo)
/assets/demo.mp4

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Environment / secrets (never commit these)
.env
.env.*
*.pem
*.key
id_rsa*
