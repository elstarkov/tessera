[package]
name = "egui_term"
version = "0.1.0"
edition = "2021"
description = "Terminal emulator widget powered by egui + alacritty_terminal (vendored & patched for tessera)."
license = "MIT"
# Upstream: https://github.com/Harzu/egui_term (MIT). Vendored and modified locally.

[lib]
name = "egui_term"
path = "src/lib.rs"

[dependencies]
alacritty_terminal = "0.25.0"
anyhow = "1.0.96"
egui = "0.31"
open = "5.3"
